<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.assignmentone.MainActivity">

    <TextView
        android:id="@+id/txtLabelID"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:gravity="center"
        android:text="@string/txtLabel"
        android:textSize="25sp" />

    <LinearLayout
        android:id="@+id/tableID"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/txtLabelID"
        android:layout_marginTop="10sp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnRegisterClickId"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_weight="50"
            android:onClick="RegisterClick"
            android:text="@string/btnRegisterClick"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btnToggleImageId"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_weight="50"
            android:onClick="ToggleImage"
            android:text="@string/btnToggleImage"
            android:textAllCaps="false" />
    </LinearLayout>

    <ImageView
        android:id="@+id/imageViewID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tableID"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20sp"
        android:adjustViewBounds="true"
        android:contentDescription="@string/imageViewDescription"
        android:src="@drawable/a_dog"
        android:visibility="invisible" />


</RelativeLayout>
